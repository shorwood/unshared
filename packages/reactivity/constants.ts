/** The symbol used to identify reactive objects. */
export const ReactiveFlag = Symbol('Reactive')

/** The symbol that stores the reactive data. */
export const ReactiveData = Symbol('ReactiveData')

/** Symbol to identify reactive references. */
export const ReferenceFlag = Symbol('Reference')

/** Symbol to identify computed references. */
export const ComputedFlag = Symbol('Computed')

/** The symbol that stores the computed data. */
export const ComputedData = Symbol('ComputedData')
