/* eslint-disable unicorn/prevent-abbreviations */
/** Matches postal code of Ascension Island. */
export const postalCodeAscensionIsland = /^ASCN 1ZZ$/

/** Matches postal code of Andorra. */
export const postalCodeAndorra = /^AD[1-7]0\d$/

/** Matches postal code of United Arab Emirates. */
export const postalCodeUnitedArabEmirates = /\w+/

/** Matches postal code of Afghanistan. */
export const postalCodeAfghanistan = /^\d{4}$/

/** Matches postal code of Antigua And Barbuda. */
export const postalCodeAntiguaAndBarbuda = /\w+/

/** Matches postal code of Anguilla. */
export const postalCodeAnguilla = /^(?:AI-)?2640$/

/** Matches postal code of Albania. */
export const postalCodeAlbania = /^\d{4}$/

/** Matches postal code of Armenia. */
export const postalCodeArmenia = /^(?:37)?\d{4}$/

/** Matches postal code of Angola. */
export const postalCodeAngola = /\w+/

/** Matches postal code of Antarctica. */
export const postalCodeAntarctica = /\w+/

/** Matches postal code of Argentina. */
export const postalCodeArgentina = /^([A-HJ-NP-Z]?\d{4})([A-Z]{3})?$/

/** Matches postal code of American Samoa. */
export const postalCodeAmericanSamoa = /^(96799)(?:[ \-](\d{4}))?$/

/** Matches postal code of Austria. */
export const postalCodeAustria = /^\d{4}$/

/** Matches postal code of Australia. */
export const postalCodeAustralia = /^\d{4}$/

/** Matches postal code of Aruba. */
export const postalCodeAruba = /\w+/

/** Matches postal code of Finland. */
export const postalCodeFinland = /^\d{5}$/

/** Matches postal code of Azerbaijan. */
export const postalCodeAzerbaijan = /^\d{4}$/

/** Matches postal code of Bosnia And Herzegovina. */
export const postalCodeBosniaAndHerzegovina = /^\d{5}$/

/** Matches postal code of Barbados. */
export const postalCodeBarbados = /^BB\d{5}$/

/** Matches postal code of Bangladesh. */
export const postalCodeBangladesh = /^\d{4}$/

/** Matches postal code of Belgium. */
export const postalCodeBelgium = /^\d{4}$/

/** Matches postal code of Burkina Faso. */
export const postalCodeBurkinaFaso = /\w+/

/** Matches postal code of Bulgaria Rep . */
export const postalCodeBulgariaRep = /^\d{4}$/

/** Matches postal code of Bahrain. */
export const postalCodeBahrain = /^(?:\d|1[0-2])\d{2}$/

/** Matches postal code of Burundi. */
export const postalCodeBurundi = /\w+/

/** Matches postal code of Benin. */
export const postalCodeBenin = /\w+/

/** Matches postal code of Saint Barthelemy. */
export const postalCodeSaintBarthelemy = /^9[78][01]\d{2}$/

/** Matches postal code of Bermuda. */
export const postalCodeBermuda = /^[A-Z]{2} ?[\dA-Z]{2}$/

/** Matches postal code of Brunei Darussalam. */
export const postalCodeBruneiDarussalam = /^[A-Z]{2} ?\d{4}$/

/** Matches postal code of Bolivia. */
export const postalCodeBolivia = /\w+/

/** Matches postal code of Bonaire Sint Eustatius And Saba. */
export const postalCodeBonaireSintEustatiusAndSaba = /\w+/

/** Matches postal code of Brazil. */
export const postalCodeBrazil = /^\d{5}-?\d{3}$/

/** Matches postal code of Bahamas. */
export const postalCodeBahamas = /\w+/

/** Matches postal code of Bhutan. */
export const postalCodeBhutan = /^\d{5}$/

/** Matches postal code of Bouvet Island. */
export const postalCodeBouvetIsland = /\w+/

/** Matches postal code of Botswana. */
export const postalCodeBotswana = /\w+/

/** Matches postal code of Belarus. */
export const postalCodeBelarus = /^\d{6}$/

/** Matches postal code of Belize. */
export const postalCodeBelize = /\w+/

/** Matches postal code of Canada. */
export const postalCodeCanada = /^[A-CEGHJ-NPR-TVXY]\d[A-CEGHJ-NPR-TV-Z] ?\d[A-CEGHJ-NPR-TV-Z]\d$/

/** Matches postal code of Cocos Keeling Islands. */
export const postalCodeCocosKeelingIslands = /^6799$/

/** Matches postal code of Congo Dem Rep . */
export const postalCodeCongoDemRep = /\w+/

/** Matches postal code of Central African Republic. */
export const postalCodeCentralAfricanRepublic = /\w+/

/** Matches postal code of Congo Rep . */
export const postalCodeCongoRep = /\w+/

/** Matches postal code of Switzerland. */
export const postalCodeSwitzerland = /^\d{4}$/

/** Matches postal code of Cote D Ivoire. */
export const postalCodeCoteDIvoire = /\w+/

/** Matches postal code of Cook Islands. */
export const postalCodeCookIslands = /\w+/

/** Matches postal code of Chile. */
export const postalCodeChile = /^\d{7}$/

/** Matches postal code of Cameroon. */
export const postalCodeCameroon = /\w+/

/** Matches postal code of China. */
export const postalCodeChina = /^\d{6}$/

/** Matches postal code of Colombia. */
export const postalCodeColombia = /^\d{6}$/

/** Matches postal code of Costa Rica. */
export const postalCodeCostaRica = /^\d{4,5}|\d{3}-\d{4}$/

/** Matches postal code of Cape Verde. */
export const postalCodeCapeVerde = /^\d{4}$/

/** Matches postal code of Curacao. */
export const postalCodeCuracao = /\w+/

/** Matches postal code of Christmas Island. */
export const postalCodeChristmasIsland = /^6798$/

/** Matches postal code of Cyprus. */
export const postalCodeCyprus = /^\d{4}$/

/** Matches postal code of Czech Rep . */
export const postalCodeCzechRep = /^\d{3} ?\d{2}$/

/** Matches postal code of Germany. */
export const postalCodeGermany = /^\d{5}$/

/** Matches postal code of Djibouti. */
export const postalCodeDjibouti = /\w+/

/** Matches postal code of Denmark. */
export const postalCodeDenmark = /^\d{4}$/

/** Matches postal code of Dominica. */
export const postalCodeDominica = /\w+/

/** Matches postal code of Dominican Rep . */
export const postalCodeDominicanRep = /^\d{5}$/

/** Matches postal code of Algeria. */
export const postalCodeAlgeria = /^\d{5}$/

/** Matches postal code of Ecuador. */
export const postalCodeEcuador = /^\d{6}$/

/** Matches postal code of Estonia. */
export const postalCodeEstonia = /^\d{5}$/

/** Matches postal code of Egypt. */
export const postalCodeEgypt = /^\d{5}$/

/** Matches postal code of Western Sahara. */
export const postalCodeWesternSahara = /^\d{5}$/

/** Matches postal code of Eritrea. */
export const postalCodeEritrea = /\w+/

/** Matches postal code of Spain. */
export const postalCodeSpain = /^\d{5}$/

/** Matches postal code of Ethiopia. */
export const postalCodeEthiopia = /^\d{4}$/

/** Matches postal code of Fiji. */
export const postalCodeFiji = /\w+/

/** Matches postal code of Falkland Islands Malvinas . */
export const postalCodeFalklandIslandsMalvinas = /^FIQQ 1ZZ$/

/** Matches postal code of Micronesia Federated State Of . */
export const postalCodeMicronesiaFederatedStateOf = /^(9694[1-4])(?:[ \-](\d{4}))?$/

/** Matches postal code of Faroe Islands. */
export const postalCodeFaroeIslands = /^\d{3}$/

/** Matches postal code of France. */
export const postalCodeFrance = /^\d{2} ?\d{3}$/

/** Matches postal code of Gabon. */
export const postalCodeGabon = /\w+/

/** Matches postal code of United Kingdom. */
export const postalCodeUnitedKingdom = /^GIR ?0AA|(?:(?:AB|AL|B|BA|BB|BD|BH|BL|BN|BR|BS|BT|BX|CA|CB|CF|CH|CM|CO|CR|CT|CV|CW|DA|DD|DE|DG|DH|DL|DN|DT|DY|E|EC|EH|EN|EX|FK|FY|G|GL|GY|GU|HA|HD|HG|HP|HR|HS|HU|HX|IG|IM|IP|IV|JE|KA|KT|KW|KY|L|LA|LD|LE|LL|LN|LS|LU|M|ME|MK|ML|N|NE|NG|NN|NP|NR|NW|OL|OX|PA|PE|PH|PL|PO|PR|RG|RH|RM|S|SA|SE|SG|SK|SL|SM|SN|SO|SP|SR|SS|ST|SW|SY|TA|TD|TF|TN|TQ|TR|TS|TW|UB|W|WA|WC|WD|WF|WN|WR|WS|WV|YO|ZE)\d[\dA-Z]? ?\d[ABD-HJLN-UW-Z]{2})|BFPO ?\d{1,4}$/

/** Matches postal code of Grenada West Indies . */
export const postalCodeGrenadaWestIndies = /\w+/

/** Matches postal code of Georgia. */
export const postalCodeGeorgia = /^\d{4}$/

/** Matches postal code of French Guiana. */
export const postalCodeFrenchGuiana = /^9[78]3\d{2}$/

/** Matches postal code of Channel Islands. */
export const postalCodeChannelIslands = /^JE\d[\dA-Z]? ?\d[ABD-HJLN-UW-Z]{2}$/

/** Matches postal code of Ghana. */
export const postalCodeGhana = /\w+/

/** Matches postal code of Gibraltar. */
export const postalCodeGibraltar = /^GX11 1AA$/

/** Matches postal code of Greenland. */
export const postalCodeGreenland = /^39\d{2}$/

/** Matches postal code of Gambia. */
export const postalCodeGambia = /\w+/

/** Matches postal code of Guinea. */
export const postalCodeGuinea = /^\d{3}$/

/** Matches postal code of Guadeloupe. */
export const postalCodeGuadeloupe = /^9[78][01]\d{2}$/

/** Matches postal code of Equatorial Guinea. */
export const postalCodeEquatorialGuinea = /\w+/

/** Matches postal code of Greece. */
export const postalCodeGreece = /^\d{3} ?\d{2}$/

/** Matches postal code of South Georgia. */
export const postalCodeSouthGeorgia = /^SIQQ 1ZZ$/

/** Matches postal code of Guatemala. */
export const postalCodeGuatemala = /^\d{5}$/

/** Matches postal code of Guam. */
export const postalCodeGuam = /^(969(?:[12]\d|3[12]))(?:[ \-](\d{4}))?$/

/** Matches postal code of Guinea Bissau. */
export const postalCodeGuineaBissau = /^\d{4}$/

/** Matches postal code of Guyana. */
export const postalCodeGuyana = /\w+/

/** Matches postal code of Hong Kong. */
export const postalCodeHongKong = /\w+/

/** Matches postal code of Heard And Mcdonald Islands. */
export const postalCodeHeardAndMcdonaldIslands = /^\d{4}$/

/** Matches postal code of Honduras. */
export const postalCodeHonduras = /^\d{5}$/

/** Matches postal code of Croatia. */
export const postalCodeCroatia = /^\d{5}$/

/** Matches postal code of Haiti. */
export const postalCodeHaiti = /^\d{4}$/

/** Matches postal code of Hungary Rep . */
export const postalCodeHungaryRep = /^\d{4}$/

/** Matches postal code of Indonesia. */
export const postalCodeIndonesia = /^\d{5}$/

/** Matches postal code of Ireland. */
export const postalCodeIreland = /^[\dA-Z]{3} ?[\dA-Z]{4}$/

/** Matches postal code of Israel. */
export const postalCodeIsrael = /^\d{5}(?:\d{2})?$/

/** Matches postal code of Isle Of Man. */
export const postalCodeIsleOfMan = /^IM\d[\dA-Z]? ?\d[ABD-HJLN-UW-Z]{2}$/

/** Matches postal code of India. */
export const postalCodeIndia = /^\d{6}$/

/** Matches postal code of British Indian Ocean Territory. */
export const postalCodeBritishIndianOceanTerritory = /^BBND 1ZZ$/

/** Matches postal code of Iraq. */
export const postalCodeIraq = /^\d{5}$/

/** Matches postal code of Iran. */
export const postalCodeIran = /^\d{5}-?\d{5}$/

/** Matches postal code of Iceland. */
export const postalCodeIceland = /^\d{3}$/

/** Matches postal code of Italy. */
export const postalCodeItaly = /^\d{5}$/

/** Matches postal code of Jamaica. */
export const postalCodeJamaica = /\w+/

/** Matches postal code of Jordan. */
export const postalCodeJordan = /^\d{5}$/

/** Matches postal code of Japan. */
export const postalCodeJapan = /^\d{3}-?\d{4}$/

/** Matches postal code of Kenya. */
export const postalCodeKenya = /^\d{5}$/

/** Matches postal code of Kyrgyzstan. */
export const postalCodeKyrgyzstan = /^\d{6}$/

/** Matches postal code of Cambodia. */
export const postalCodeCambodia = /^\d{5}$/

/** Matches postal code of Kiribati. */
export const postalCodeKiribati = /\w+/

/** Matches postal code of Comoros. */
export const postalCodeComoros = /\w+/

/** Matches postal code of Saint Kitts And Nevis. */
export const postalCodeSaintKittsAndNevis = /\w+/

/** Matches postal code of South Korea. */
export const postalCodeSouthKorea = /^\d{5}$/

/** Matches postal code of Kuwait. */
export const postalCodeKuwait = /^\d{5}$/

/** Matches postal code of Cayman Islands. */
export const postalCodeCaymanIslands = /^KY\d-\d{4}$/

/** Matches postal code of Kazakhstan. */
export const postalCodeKazakhstan = /^\d{6}$/

/** Matches postal code of Lao People S Dem Rep . */
export const postalCodeLaoPeopleSDemRep = /^\d{5}$/

/** Matches postal code of Lebanon. */
export const postalCodeLebanon = /^\d{4}(?: ?\d{4})?$/

/** Matches postal code of Saint Lucia. */
export const postalCodeSaintLucia = /\w+/

/** Matches postal code of Liechtenstein. */
export const postalCodeLiechtenstein = /^948[5-9]|949[0-7]$/

/** Matches postal code of Sri Lanka. */
export const postalCodeSriLanka = /^\d{5}$/

/** Matches postal code of Liberia. */
export const postalCodeLiberia = /^\d{4}$/

/** Matches postal code of Lesotho. */
export const postalCodeLesotho = /^\d{3}$/

/** Matches postal code of Lithuania. */
export const postalCodeLithuania = /^\d{5}$/

/** Matches postal code of Luxembourg. */
export const postalCodeLuxembourg = /^\d{4}$/

/** Matches postal code of Latvia. */
export const postalCodeLatvia = /^LV-\d{4}$/

/** Matches postal code of Libya. */
export const postalCodeLibya = /\w+/

/** Matches postal code of Morocco. */
export const postalCodeMorocco = /^\d{5}$/

/** Matches postal code of Monaco. */
export const postalCodeMonaco = /^980\d{2}$/

/** Matches postal code of Rep Moldova. */
export const postalCodeRepMoldova = /^\d{4}$/

/** Matches postal code of Montenegro. */
export const postalCodeMontenegro = /^8\d{4}$/

/** Matches postal code of Saint Martin. */
export const postalCodeSaintMartin = /^9[78][01]\d{2}$/

/** Matches postal code of Madagascar. */
export const postalCodeMadagascar = /^\d{3}$/

/** Matches postal code of Marshall Islands. */
export const postalCodeMarshallIslands = /^(969[67]\d)(?:[ \-](\d{4}))?$/

/** Matches postal code of Macedonia. */
export const postalCodeMacedonia = /^\d{4}$/

/** Matches postal code of Mali. */
export const postalCodeMali = /\w+/

/** Matches postal code of Myanmar. */
export const postalCodeMyanmar = /^\d{5}$/

/** Matches postal code of Mongolia. */
export const postalCodeMongolia = /^\d{5}$/

/** Matches postal code of Macao. */
export const postalCodeMacao = /\w+/

/** Matches postal code of Northern Mariana Islands. */
export const postalCodeNorthernMarianaIslands = /^(9695[0-2])(?:[ \-](\d{4}))?$/

/** Matches postal code of Martinique. */
export const postalCodeMartinique = /^9[78]2\d{2}$/

/** Matches postal code of Mauritania. */
export const postalCodeMauritania = /\w+/

/** Matches postal code of Montserrat. */
export const postalCodeMontserrat = /\w+/

/** Matches postal code of Malta. */
export const postalCodeMalta = /^[A-Z]{3} ?\d{2,4}$/

/** Matches postal code of Mauritius. */
export const postalCodeMauritius = /^\d{3}(?:\d{2}|[A-Z]{2}\d{3})$/

/** Matches postal code of Maldives. */
export const postalCodeMaldives = /^\d{5}$/

/** Matches postal code of Malawi. */
export const postalCodeMalawi = /\w+/

/** Matches postal code of Mexico. */
export const postalCodeMexico = /^\d{5}$/

/** Matches postal code of Malaysia. */
export const postalCodeMalaysia = /^\d{5}$/

/** Matches postal code of Mozambique. */
export const postalCodeMozambique = /^\d{4}$/

/** Matches postal code of Namibia. */
export const postalCodeNamibia = /\w+/

/** Matches postal code of New Caledonia. */
export const postalCodeNewCaledonia = /^988\d{2}$/

/** Matches postal code of Niger. */
export const postalCodeNiger = /^\d{4}$/

/** Matches postal code of Norfolk Island. */
export const postalCodeNorfolkIsland = /^2899$/

/** Matches postal code of Nigeria. */
export const postalCodeNigeria = /^\d{6}$/

/** Matches postal code of Nicaragua. */
export const postalCodeNicaragua = /^\d{5}$/

/** Matches postal code of Netherlands. */
export const postalCodeNetherlands = /^\d{4} ?[A-Z]{2}$/

/** Matches postal code of Norway. */
export const postalCodeNorway = /^\d{4}$/

/** Matches postal code of Nepal. */
export const postalCodeNepal = /^\d{5}$/

/** Matches postal code of Nauru Central Pacific. */
export const postalCodeNauruCentralPacific = /\w+/

/** Matches postal code of Niue. */
export const postalCodeNiue = /\w+/

/** Matches postal code of New Zealand. */
export const postalCodeNewZealand = /^\d{4}$/

/** Matches postal code of Oman. */
export const postalCodeOman = /^(?:PC )?\d{3}$/

/** Matches postal code of Panama Rep . */
export const postalCodePanamaRep = /\w+/

/** Matches postal code of Peru. */
export const postalCodePeru = /^(?:LIMA \d{1,2}|CALLAO 0?\d)|[0-2]\d{4}$/

/** Matches postal code of French Polynesia. */
export const postalCodeFrenchPolynesia = /^987\d{2}$/

/** Matches postal code of Papua New Guinea. */
export const postalCodePapuaNewGuinea = /^\d{3}$/

/** Matches postal code of Philippines. */
export const postalCodePhilippines = /^\d{4}$/

/** Matches postal code of Pakistan. */
export const postalCodePakistan = /^\d{5}$/

/** Matches postal code of Poland. */
export const postalCodePoland = /^\d{2}-\d{3}$/

/** Matches postal code of St Pierre And Miquelon. */
export const postalCodeStPierreAndMiquelon = /^9[78]5\d{2}$/

/** Matches postal code of Pitcairn. */
export const postalCodePitcairn = /^PCRN 1ZZ$/

/** Matches postal code of Puerto Rico. */
export const postalCodePuertoRico = /^(00[679]\d{2})(?:[ \-](\d{4}))?$/

/** Matches postal code of Palestinian Territory. */
export const postalCodePalestinianTerritory = /\w+/

/** Matches postal code of Portugal. */
export const postalCodePortugal = /^\d{4}-\d{3}$/

/** Matches postal code of Palau. */
export const postalCodePalau = /^(969(?:39|40))(?:[ \-](\d{4}))?$/

/** Matches postal code of Paraguay. */
export const postalCodeParaguay = /^\d{4}$/

/** Matches postal code of Qatar. */
export const postalCodeQatar = /\w+/

/** Matches postal code of Reunion. */
export const postalCodeReunion = /^9[78]4\d{2}$/

/** Matches postal code of Romania. */
export const postalCodeRomania = /^\d{6}$/

/** Matches postal code of Republic Of Serbia. */
export const postalCodeRepublicOfSerbia = /^\d{5,6}$/

/** Matches postal code of Russian Federation. */
export const postalCodeRussianFederation = /^\d{6}$/

/** Matches postal code of Rwanda. */
export const postalCodeRwanda = /\w+/

/** Matches postal code of Saudi Arabia. */
export const postalCodeSaudiArabia = /^\d{5}$/

/** Matches postal code of Solomon Islands. */
export const postalCodeSolomonIslands = /\w+/

/** Matches postal code of Seychelles. */
export const postalCodeSeychelles = /\w+/

/** Matches postal code of Sweden. */
export const postalCodeSweden = /^\d{3} ?\d{2}$/

/** Matches postal code of Rep Of Singapore. */
export const postalCodeRepOfSingapore = /^\d{6}$/

/** Matches postal code of Saint Helena. */
export const postalCodeSaintHelena = /^(?:ASCN|STHL) 1ZZ$/

/** Matches postal code of Slovenia. */
export const postalCodeSlovenia = /^\d{4}$/

/** Matches postal code of Svalbard And Jan Mayen Islands. */
export const postalCodeSvalbardAndJanMayenIslands = /^\d{4}$/

/** Matches postal code of Slovakia. */
export const postalCodeSlovakia = /^\d{3} ?\d{2}$/

/** Matches postal code of Sierra Leone. */
export const postalCodeSierraLeone = /\w+/

/** Matches postal code of San Marino. */
export const postalCodeSanMarino = /^4789\d$/

/** Matches postal code of Senegal. */
export const postalCodeSenegal = /^\d{5}$/

/** Matches postal code of Somalia. */
export const postalCodeSomalia = /^[A-Z]{2} ?\d{5}$/

/** Matches postal code of Suriname. */
export const postalCodeSuriname = /\w+/

/** Matches postal code of South Sudan. */
export const postalCodeSouthSudan = /\w+/

/** Matches postal code of Sao Tome And Principe. */
export const postalCodeSaoTomeAndPrincipe = /\w+/

/** Matches postal code of El Salvador. */
export const postalCodeElSalvador = /^CP [1-3][1-7][0-2]\d$/

/** Matches postal code of Sint Maarten. */
export const postalCodeSintMaarten = /\w+/

/** Matches postal code of Swaziland. */
export const postalCodeSwaziland = /^[HLMS]\d{3}$/

/** Matches postal code of Tristan Da Cunha. */
export const postalCodeTristanDaCunha = /^TDCU 1ZZ$/

/** Matches postal code of Turks And Caicos Islands. */
export const postalCodeTurksAndCaicosIslands = /^TKCA 1ZZ$/

/** Matches postal code of Chad. */
export const postalCodeChad = /\w+/

/** Matches postal code of French Southern Territories. */
export const postalCodeFrenchSouthernTerritories = /\w+/

/** Matches postal code of Togo. */
export const postalCodeTogo = /\w+/

/** Matches postal code of Thailand. */
export const postalCodeThailand = /^\d{5}$/

/** Matches postal code of Tajikistan. */
export const postalCodeTajikistan = /^\d{6}$/

/** Matches postal code of Tokelau. */
export const postalCodeTokelau = /\w+/

/** Matches postal code of Timor Leste. */
export const postalCodeTimorLeste = /\w+/

/** Matches postal code of Turkmenistan. */
export const postalCodeTurkmenistan = /^\d{6}$/

/** Matches postal code of Tunisia. */
export const postalCodeTunisia = /^\d{4}$/

/** Matches postal code of Tonga. */
export const postalCodeTonga = /\w+/

/** Matches postal code of Turkey. */
export const postalCodeTurkey = /^\d{5}$/

/** Matches postal code of Trinidad And Tobago. */
export const postalCodeTrinidadAndTobago = /\w+/

/** Matches postal code of Tuvalu. */
export const postalCodeTuvalu = /\w+/

/** Matches postal code of Taiwan. */
export const postalCodeTaiwan = /^\d{3}(?:\d{2})?$/

/** Matches postal code of Tanzania United Rep . */
export const postalCodeTanzaniaUnitedRep = /^\d{4,5}$/

/** Matches postal code of Ukraine. */
export const postalCodeUkraine = /^\d{5}$/

/** Matches postal code of Uganda. */
export const postalCodeUganda = /\w+/

/** Matches postal code of United States Minor Outlying Islands. */
export const postalCodeUnitedStatesMinorOutlyingIslands = /^96898$/

/** Matches postal code of United States. */
export const postalCodeUnitedStates = /^(\d{5})(?:[ \-](\d{4}))?$/

/** Matches postal code of Uruguay. */
export const postalCodeUruguay = /^\d{5}$/

/** Matches postal code of Uzbekistan. */
export const postalCodeUzbekistan = /^\d{6}$/

/** Matches postal code of Vatican. */
export const postalCodeVatican = /^00120$/

/** Matches postal code of Saint Vincent And The Grenadines Antilles . */
export const postalCodeSaintVincentAndTheGrenadinesAntilles = /^VC\d{4}$/

/** Matches postal code of Venezuela. */
export const postalCodeVenezuela = /^\d{4}$/

/** Matches postal code of Virgin Islands British . */
export const postalCodeVirginIslandsBritish = /^VG\d{4}$/

/** Matches postal code of Virgin Islands U S . */
export const postalCodeVirginIslandsUS = /^(008(?:(?:[0-4]\d)|(?:5[01])))(?:[ \-](\d{4}))?$/

/** Matches postal code of Viet Nam. */
export const postalCodeVietNam = /^\d{6}$/

/** Matches postal code of Vanuatu. */
export const postalCodeVanuatu = /\w+/

/** Matches postal code of Wallis And Futuna Islands. */
export const postalCodeWallisAndFutunaIslands = /^986\d{2}$/

/** Matches postal code of Samoa. */
export const postalCodeSamoa = /\w+/

/** Matches postal code of Kosovo. */
export const postalCodeKosovo = /^[1-7]\d{4}$/

/** Matches postal code of Yemen. */
export const postalCodeYemen = /\w+/

/** Matches postal code of Mayotte. */
export const postalCodeMayotte = /^976\d{2}$/

/** Matches postal code of South Africa. */
export const postalCodeSouthAfrica = /^\d{4}$/

/** Matches postal code of Zambia. */
export const postalCodeZambia = /^\d{5}$/

/** Matches postal code of Zimbabwe. */
export const postalCodeZimbabwe = /\w+/
