export interface LogPino {
  /**
   * The name of the logger. When set adds a `name` field to every JSON line logged.
   *
   * @see https://github.com/pinojs/pino/blob/master/docs/api.md#name-string
   * @example "Application"
   */
  // name: string
}
